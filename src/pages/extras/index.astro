---
import Layout from '../../components/Layout.astro';
import { withBase } from '../../lib/paths';

const title = 'Extras';
const description = 'Extra tools, activities and supporting resources.';
---

<Layout title={title} description={description}>
  <h1 class="font-display text-3xl mb-6">{title}</h1>

  <ul class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 list-none p-0 m-0">
    <li class="border border-fg/20 p-4">
      <a class="font-semibold hover:text-neon" href={withBase('extras/glossary/')}>Glossary</a>
      <p class="text-sm text-muted mt-1">Common terms, slang, and rules you’ll see across games.</p>
    </li>
    <li class="border border-fg/20 p-4">
      <a class="font-semibold hover:text-neon" href={withBase('extras/forfeits/')}>Forfeits</a>
      <p class="text-sm text-muted mt-1">Punishments and challenges to spice up any game.</p>
    </li>
    <li class="border border-fg/20 p-4">
      <a class="font-semibold hover:text-neon" href={withBase('extras/wheel-of-fortune/')}>Wheel of Fortune</a>
      <p class="text-sm text-muted mt-1">Spin up random dares and actions for the group.</p>
    </li>
    <li class="border border-fg/20 p-4">
      <a class="font-semibold hover:text-neon" href={withBase('activities/')}>Activities &amp; Minigames</a>
      <p class="text-sm text-muted mt-1">Quick activities and icebreakers to keep the party moving.</p>
    </li>
    <li class="border border-fg/20 p-4">
      <a class="font-semibold hover:text-neon" href={withBase('blog/')}>Blog</a>
      <p class="text-sm text-muted mt-1">Guides, how‑tos and party inspiration.</p>
    </li>
  </ul>
</Layout>
